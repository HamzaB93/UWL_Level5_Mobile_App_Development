<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bitsandpizzas.hfad.bitsandpizzas" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OrderActivity"
            android:label="@string/title_activity_order" >
        </activity>
    </application>

</manifest><!--
 icon attribute used for the icon for the app eg in the phone try and app action bar
    Can be a drawable or mipmap resource.
-->
<!--
 label gives a user friendly lable to the activity depending if its in the
    <application> or <activity> attribute
-->
<!--
 theme specifies the theme, @style prefix means the theme is defined in a style resource file
    Can define the style in style resource files
-->
